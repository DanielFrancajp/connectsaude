import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        justifyContent: 'center',
        alignItems:'center'
    },
    contentTitle: {
        marginTop: 20,
        width: '100%',
        alignItems: 'center',
        paddingBottom: 10
    },
    Text: {
        fontSize: 16,
        fontWeight: 'bold',
        color: '#0B80C8'
    },
})